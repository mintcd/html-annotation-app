const annotationWrapperStyles = {
  contentContainer: (contentShown: boolean): React.CSSProperties => ({
    // display: contentShown ? 'block' : 'none'
    display: 'block',
  })
};

export default annotationWrapperStyles;